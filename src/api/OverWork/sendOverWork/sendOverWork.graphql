type Mutation {
  # sendOverWork(email: String! title: String!, content: String!): Boolean
  sendOverWork(email: String! yearMonth: String! orderBy: OverworkOrderByInput): Boolean
}

input OverworkOrderByInput {
  date: Sort
}

enum Sort {
  asc
  desc
}
