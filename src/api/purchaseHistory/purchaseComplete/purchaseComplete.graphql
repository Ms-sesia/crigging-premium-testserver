type Mutation {
  purchaseComplete(
    phoneNumber: String!
    craneNames: [String!]!
    price: Int!
    date: String
    cardCompany: String
    installment: String
    receipt: String
  ): Boolean
}
