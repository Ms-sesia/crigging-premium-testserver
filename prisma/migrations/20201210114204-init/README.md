# Migration `20201210114204-init`

This migration has been generated by sesia at 12/10/2020, 8:42:04 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `OverWork` ALTER COLUMN `updatedAt` DROP DEFAULT

ALTER TABLE `Post` MODIFY `postCategory` ENUM('JOB', 'REPAIR', 'ETC') NOT NULL,
    ALTER COLUMN `updatedAt` DROP DEFAULT

ALTER TABLE `User` ALTER COLUMN `updatedAt` DROP DEFAULT
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201210110117-init..20201210114204-init
--- datamodel.dml
+++ datamodel.dml
@@ -3,15 +3,15 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 model User {
   id              Int             @id @default(autoincrement())
   createdAt       DateTime        @default(now())
-  updatedAt       DateTime        @default(now())
+  updatedAt       DateTime        @updatedAt
   name            String          @unique
   email           String          @unique
   phoneNumber     String          @unique
   loginSecretCode String?
@@ -27,9 +27,9 @@
 model Post {
   id           Int          @id @default(autoincrement())
   createdAt    DateTime     @default(now())
-  updatedAt    DateTime     @default(now())
+  updatedAt    DateTime     @updatedAt
   title        String
   text         String?
   postCategory PostCategory
   comments     Comment[]
@@ -82,9 +82,9 @@
   amount           Int
   author           User             @relation(fields: [authorId], references: [id])
   authorId         Int
   createdAt        DateTime         @default(now())
-  updatedAt        DateTime         @default(now())
+  updatedAt        DateTime         @updatedAt
 }
 model File {
   id     Int    @id @default(autoincrement())
```


